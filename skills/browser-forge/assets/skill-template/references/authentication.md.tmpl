# Authentication

This skill targets {{TARGET_DOMAINS_JSON}} and records auth runtime version
`{{AUTH_RUNTIME_VERSION}}` in its manifest. Add only redacted authentication
instructions; never persist session material.
