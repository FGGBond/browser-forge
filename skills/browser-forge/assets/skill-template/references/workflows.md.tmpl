# Workflows

Document recording-confirmed request sequences here. Do not copy recording
files, credentials, cookies, tokens, or personal data into this skill.

Declare each request as `METHOD /path`, give every step a unique ID, and list
only dependencies that appear earlier in the command. Put optional sanitized
JSON payload templates in the step's `body` field.
