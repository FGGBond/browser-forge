# Example command

Replace this placeholder with a sanitized business-command contract. Describe
inputs, outputs, dependencies, side effects, idempotency, and next actions.
For a side effect, also document the manifest's dry-run support, retry risk,
and verification behavior.
