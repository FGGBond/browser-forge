#!/usr/bin/env bash
set -euo pipefail

script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
repository_root="$(cd "$script_dir/../../.." && pwd)"
exec node "$repository_root/src/skill-generation/cli.mjs" generate "$@"
